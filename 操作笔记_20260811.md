# 操作笔记（2026-08-11）

## 一、本次做了什么

### 1. 审核并修复"网页自选股不能用"

- 根因：`report_server.py` 添加自选股分支只更新内存列表，**不写回 `自选股.txt`、也不返回响应**，导致网页添加一直失败。
- 修复内容：
  - [report_server.py](report_server.py:197)：添加分支补上"写回文件 + 重新生成报告 + 返回响应"；
  - [report_server.py](report_server.py:211)：移除分支容错（数据先删，报告生成失败也返回成功提示）；
  - [daily_report.py](daily_report.py:141)：跟踪表对空值价格显示"—"，行情接口故障时不再崩溃。
- 验证：本地启动服务实测 添加/移除/刷新 全部 200 正常。

### 2. 初始化 Git 仓库

- 在项目文件夹执行 `git init`，创建首个提交：
  - 提交号：`0a7cf5b`
  - 提交内容：36 个源码/配置文件（5252 行）
- 新增 `.gitignore`，排除隐私与生成物，**未上传**：
  - 个人数据：`自选股.txt`、`journal.db`、`选股数据.db`
  - 生成报告/日志：`每日量化选股报告_*.html`、`每日热门股_*.txt`、`组合与执行报告.html`、`报告归档/`、日志 txt
  - 缓存/运行时：`排名历史.json`、`热门股缓存.json`、`网页服务.pid`
  - 第三方技能库：`cnfinancialscraper/`
  - Python/编辑器杂项：`__pycache__/`、`.vscode/` 等

### 3. 推送到 GitHub

- 远程仓库：https://github.com/yeliming0576/git
- 分支：`master`（已跟踪 `origin/master`）
- 提交身份：叶黎明0576 <yeliming0576@163.com>
- 推送命令：`git push -u origin master`

## 二、常用操作备忘

| 操作 | 方法 |
|---|---|
| 一键生成今日报告 | 双击 `一键运行_带提示.vbs` |
| 启动网页服务（管理自选股） | 双击 `启动报告服务.cmd`，浏览器打开 http://127.0.0.1:8765/ |
| 停止网页服务 | 双击 `停止网页服务.cmd`（或直接关服务窗口，但会断开局域网访问） |
| 提交代码 | `git add -A` → `git commit -m "说明"` → `git push` |

## 三、注意事项

- 网页服务是"窗口在，服务在"：关掉命令行窗口，局域网其他人也无法访问。
- 添加/移除自选股约 10~60 秒属正常（需要联网重新生成报告）。
- 行情接口偶尔不可用属正常，程序会自动用最近一次缓存降级。
- 推送 GitHub 前务必确认 `.gitignore` 仍生效（`git status` 检查没有隐私文件）。
